<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For My Future Wife 💖</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: #fff;
      text-align: center;
      overflow-x: hidden;
      min-height: 100vh;
    }
    header { padding: 40px 16px 10px; }
    h1 { font-size: clamp(28px, 4vw, 44px); margin: 0 0 6px; }
    p.lead { opacity: .95; margin: 0 0 24px; font-size: clamp(14px, 2.4vw, 18px); }

    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 20px;
      padding: 24px 20px 56px;
      max-width: 1100px;
      margin: 0 auto;
    }
    .item {
      background: rgba(255,255,255,0.18);
      backdrop-filter: blur(4px);
      border-radius: 18px;
      box-shadow: 0 10px 20px rgba(0,0,0,.15);
      padding: 12px;
      transition: transform .25s ease;
    }
    .item:hover { transform: translateY(-4px); }
    .item img, .item video {
      width: 100%;
      height: 260px;
      object-fit: cover;
      border-radius: 12px;
      display: block;
    }
    .caption { margin-top: 10px; font-size: 14px; line-height: 1.35; }

    .heart {
      position: fixed;
      bottom: -24px;
      font-size: 18px;
      animation: floatUp linear infinite;
      opacity: 0.75;
      user-select: none;
      pointer-events: none;
    }
    @keyframes floatUp {
      0% { transform: translateY(0) scale(1); opacity: .0; }
      8% { opacity: .85; }
      100% { transform: translateY(-105vh) scale(1.6) rotate(8deg); opacity: 0; }
    }

    footer { padding: 20px 0 26px; font-size: 35px; opacity: .9; }
  </style>
</head>
<body>
  <header>
    <h1>For Princes In My World 💕</h1>
    <p class="lead">This love isn’t just about two hearts beating as one, it’s about creating a lifetime where every moment shared becomes eternity✨</p>
  </header>

  <section class="gallery">
    <div class="item">
      <img src="firstlove.jpg" alt="Kita berdua">
      <div class="caption">First Meet With Princes</div>
    </div>

    <div class="item">
      <img src="love2.png" alt="Momen manis">
      <div class="caption">Meet With Botak</div>
    </div>

    <div class="item">
      <video controls>
        <source src="vidiolove.mp4" type="video/mp4">
        Browser kamu tidak mendukung video tag.
      </video>
      <div class="caption">Every photo tells a chapter, but together they write the story of us a love that keeps growing</div>
    </div>

    <div class="item">
      <img src="lovetext.jpeg" alt="Kenangan indah">
      <div class="caption">Read this article, princesses</div>
    </div>
  </section>

  <footer>Even when the stars burn out, my love will still shine for you❤️</footer>

  <script>
    function createHeart() {
      const heart = document.createElement('div');
      heart.className = 'heart';
      heart.textContent = Math.random() < 0.3 ? '💖' : (Math.random() < 0.6 ? '❤️' : '💕');
      heart.style.left = Math.random() * 100 + 'vw';
      const duration = 3 + Math.random() * 4;
      heart.style.animationDuration = duration + 's';
      const size = 14 + Math.floor(Math.random() * 18);
      heart.style.fontSize = size + 'px';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), duration * 1000);
    }
    const interval = setInterval(createHeart, 400);
    document.addEventListener('visibilitychange', () => {
      if (document.hidden) clearInterval(interval);
    });
  </script>
</body>
</html>
